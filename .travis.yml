language: c

compiler:
  - gcc

addons:
  apt:
    sources:
    - sourceline: 'ppa:deadsnakes/ppa'
    - sourceline: 'ppa:duggan/bats'
    packages:
    - bats
    - python3.3
    - python3.4
    - python3.5
    - python3.6
    - python3.7

before_script:
  autoreconf --install

script:
  ./configure && make && make check

after_failure:
  - pwd
  - ls
  - cat test-suite.log
